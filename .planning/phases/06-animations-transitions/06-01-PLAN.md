---
phase: 06-animations-transitions
plan: 01
type: execute
---

<objective>
Add micro-interactions to Button and Input components for tactile feedback.

Purpose: Improve perceived responsiveness with subtle scale/spring animations on user interaction.
Output: Enhanced Button.jsx and Input.jsx with framer-motion micro-interactions that respect reduceMotion preference.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Key files for this plan:
@frontend/src/components/shared/Button.jsx
@frontend/src/components/shared/Input.jsx
@frontend/src/contexts/AccessibilityContext.jsx

# Established patterns (from 01-volunteer-swiper):
- Use `useAccessibility()` hook for reduceMotion preference
- Spring transitions: `{ type: 'spring', stiffness: 260, damping: 20 }`
- Instant transitions for reduceMotion: `{ type: 'tween', duration: 0.01 }`
- Import motion from 'framer-motion'

**Tech stack available:** framer-motion 10.16.16
**Established patterns:** SwipeableCard, MatchAnimation for motion patterns
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add micro-interactions to Button component</name>
  <files>frontend/src/components/shared/Button.jsx</files>
  <action>
    Convert Button to use framer-motion `motion.button` with:
    - whileTap: scale 0.97 for press feedback
    - whileHover: scale 1.02 for subtle lift effect
    - Transition: spring with stiffness 400, damping 25 for snappy feel
    - Respect reduceMotion from AccessibilityContext - use instant transitions if true
    - Keep all existing className styling (colors, sizes, disabled states)
    - Keep loading spinner functionality
    - Import useAccessibility hook

    Do NOT change the visual appearance (colors, sizes, borders) - only add motion behavior.
    WHY: Motion should enhance, not replace, the existing design system.
  </action>
  <verify>
    1. Run: npm run dev (in frontend/)
    2. Visit localhost:5173/auth
    3. Hover over "Sign In" button - should slightly scale up
    4. Click button - should compress slightly then release
    5. Toggle reduce motion in accessibility menu - animations should become instant
  </verify>
  <done>Button has hover and tap micro-interactions, respects reduceMotion, all existing functionality preserved</done>
</task>

<task type="auto">
  <name>Task 2: Add focus animation to Input component</name>
  <files>frontend/src/components/shared/Input.jsx</files>
  <action>
    Convert Input wrapper div to use framer-motion `motion.div` with:
    - animate prop based on isFocused state (add useState for focus tracking)
    - When focused: subtle scale 1.01 for "lift" effect
    - When not focused: scale 1
    - Add onFocus/onBlur handlers to track focus state
    - Transition: spring with stiffness 300, damping 30
    - Respect reduceMotion from AccessibilityContext
    - Keep all existing className styling and error states

    Alternative simpler approach: Just add CSS transition with scale transform via Tailwind classes:
    - focus-within:scale-[1.01] on wrapper div
    - transition-transform duration-150

    Use the CSS approach if simpler - framer-motion may be overkill for inputs.
    WHY: Inputs need subtle feedback, not dramatic animations.
  </action>
  <verify>
    1. Run: npm run dev
    2. Visit localhost:5173/auth
    3. Click into email input - wrapper should subtly scale up
    4. Tab to password input - same effect
    5. Click outside - should scale back to normal
  </verify>
  <done>Input has subtle focus animation, respects reduceMotion, all existing functionality preserved</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors in frontend/
- [ ] Button micro-interactions visible on hover and tap
- [ ] Input focus animation visible when focusing fields
- [ ] Reduce motion toggle disables/minimizes animations
- [ ] No visual regressions (colors, sizes, spacing unchanged)
</verification>

<success_criteria>

- Both tasks completed
- All verification checks pass
- No TypeScript/ESLint errors
- Micro-interactions feel snappy (not sluggish)
- Accessibility preserved (reduceMotion respected)
</success_criteria>

<output>
After completion, create `.planning/phases/06-animations-transitions/06-01-SUMMARY.md`
</output>
