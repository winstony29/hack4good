---
phase: 01-volunteer-swiper
plan: 01
type: execute
---

<objective>
Set up dependencies and environment configuration for the volunteer swiper feature.

Purpose: Install Framer Motion and canvas-confetti, create environment-based mock toggle
Output: Working env.js utility, .env file, dependencies installed
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@.planning/PERSON3-VOLUNTEER-SWIPER-DESIGN.md
@frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install animation dependencies</name>
  <files>frontend/package.json</files>
  <action>
    Run `npm install framer-motion canvas-confetti` in the frontend directory.

    framer-motion: Provides drag gestures, spring animations, AnimatePresence for exit animations
    canvas-confetti: Lightweight confetti effect for match celebration

    Verify both packages are added to dependencies in package.json.
  </action>
  <verify>
    - `cat frontend/package.json | grep framer-motion` shows version
    - `cat frontend/package.json | grep canvas-confetti` shows version
    - `npm ls framer-motion canvas-confetti` in frontend/ shows both installed
  </verify>
  <done>Both framer-motion and canvas-confetti appear in package.json dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create environment utilities and .env file</name>
  <files>frontend/src/utils/env.js, frontend/.env</files>
  <action>
    Create `frontend/src/utils/env.js`:
    ```javascript
    /**
     * Environment utilities
     * Centralized environment variable access
     */

    // Mock data toggle - set VITE_USE_MOCK_DATA=true in .env for development
    export const USE_MOCK_DATA = import.meta.env.VITE_USE_MOCK_DATA === 'true'

    // API base URL (for future use)
    export const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'http://localhost:8000'
    ```

    Create `frontend/.env`:
    ```
    # Development environment variables
    # Set to 'true' to use mock data instead of backend API
    VITE_USE_MOCK_DATA=true

    # Backend API URL (used when VITE_USE_MOCK_DATA=false)
    VITE_API_BASE_URL=http://localhost:8000
    ```

    Note: Vite requires VITE_ prefix for client-side env vars. The `import.meta.env` pattern is Vite-specific.
  </action>
  <verify>
    - File `frontend/src/utils/env.js` exists with USE_MOCK_DATA export
    - File `frontend/.env` exists with VITE_USE_MOCK_DATA=true
    - `npm run dev` in frontend/ starts without errors
  </verify>
  <done>env.js exports USE_MOCK_DATA, .env sets it to true, dev server runs</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm ls framer-motion canvas-confetti` shows both packages installed
- [ ] `frontend/src/utils/env.js` exists and exports USE_MOCK_DATA
- [ ] `frontend/.env` exists with VITE_USE_MOCK_DATA=true
- [ ] `npm run dev` starts successfully
</verification>

<success_criteria>
- All tasks completed
- Dependencies installed
- Environment config created
- Dev server runs without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-volunteer-swiper/01-01-SUMMARY.md`
</output>
