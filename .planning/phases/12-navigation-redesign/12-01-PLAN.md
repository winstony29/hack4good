---
phase: 12-navigation-redesign
plan: 01
type: execute
---

<objective>
Redesign the Navbar component with warm cream background, prominent CTAs, and improved mobile navigation.

Purpose: Create welcoming, accessible navigation that matches the inclusive design aesthetic.
Output: Redesigned Navbar with coral accent, warm styling, large touch targets, mobile hamburger menu.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-accessibility-enhancement/11-02-SUMMARY.md

**Design Specification:**
Navigation:
- Sticky header with warm cream background
- Large, well-spaced nav links
- Prominent "Sign Up" / "Register" CTA button (coral)
- Mobile: hamburger menu with large touch targets (48px min)

@frontend/src/components/layout/Navbar.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle desktop navigation</name>
  <files>frontend/src/components/layout/Navbar.jsx</files>
  <action>
Update the Navbar component with warm inclusive design styling.

Update the main nav element:
```jsx
<nav className="sticky top-0 z-40 bg-minds-cream/95 backdrop-blur-sm shadow-sm border-b border-minds-border">
```

Update the logo section - use a friendlier, warmer design:
```jsx
<Link to="/" className="flex items-center gap-3">
  <div className="w-11 h-11 bg-minds-coral rounded-xl flex items-center justify-center shadow-md">
    <span className="text-white font-bold text-2xl">M</span>
  </div>
  <span className="text-xl font-bold text-minds-charcoal tracking-tight">
    {t('landing.appName')}
  </span>
</Link>
```

Update desktop nav links to use warm styling with larger touch targets:
```jsx
<Link
  to="/dashboard"
  className="px-4 py-2.5 text-minds-charcoal hover:text-minds-coral hover:bg-white/50 rounded-lg transition-all font-medium"
>
  {t('nav.dashboard')}
</Link>
```

Update the accessibility button:
```jsx
<button
  onClick={() => setAccessibilityMenuOpen(true)}
  className="p-3 text-minds-charcoal hover:text-minds-coral hover:bg-white/50 rounded-lg transition-colors"
  aria-label={t('nav.accessibility')}
>
  <Accessibility size={22} />
</button>
```

Update the logout/CTA buttons to use the new Button variants:
```jsx
<Button
  variant="primary"
  size="medium"
  onClick={() => navigate('/auth')}
>
  {t('nav.login')}
</Button>
```
  </action>
  <verify>Desktop nav shows warm cream background, coral logo, coral CTAs, larger touch targets</verify>
  <done>Desktop nav restyled with warm cream background, coral accents, 48px+ touch targets</done>
</task>

<task type="auto">
  <name>Task 2: Improve mobile navigation</name>
  <files>frontend/src/components/layout/Navbar.jsx</files>
  <action>
Update the mobile navigation section with better touch targets and warm styling.

Update mobile menu button (hamburger):
```jsx
<button
  className="p-3 text-minds-charcoal hover:text-minds-coral hover:bg-white/50 rounded-lg transition-colors min-w-[48px] min-h-[48px] flex items-center justify-center"
  onClick={() => setMobileMenuOpen(!mobileMenuOpen)}
  aria-label={mobileMenuOpen ? 'Close menu' : 'Open menu'}
  aria-expanded={mobileMenuOpen}
>
  {mobileMenuOpen ? <X size={24} /> : <Menu size={24} />}
</button>
```

Update mobile menu dropdown with warm styling and large touch targets:
```jsx
{mobileMenuOpen && (
  <motion.div
    initial={{ opacity: 0, y: -10 }}
    animate={{ opacity: 1, y: 0 }}
    exit={{ opacity: 0, y: -10 }}
    className="md:hidden absolute top-full left-0 right-0 bg-white shadow-lg border-b border-minds-border"
  >
    <div className="container mx-auto px-4 py-4 flex flex-col gap-2">
      {user ? (
        <>
          <Link
            to="/dashboard"
            className="flex items-center gap-3 px-4 py-4 text-minds-charcoal hover:bg-minds-cream rounded-xl transition-colors font-medium min-h-[56px]"
            onClick={() => setMobileMenuOpen(false)}
          >
            {t('nav.dashboard')}
          </Link>
          {/* ... other links with same styling ... */}
          <Button
            variant="outline"
            fullWidth
            size="large"
            onClick={() => {
              handleLogout()
              setMobileMenuOpen(false)
            }}
          >
            <LogOut size={20} />
            {t('nav.logout')}
          </Button>
        </>
      ) : (
        <>
          <Link
            to="/activities"
            className="flex items-center gap-3 px-4 py-4 text-minds-charcoal hover:bg-minds-cream rounded-xl transition-colors font-medium min-h-[56px]"
            onClick={() => setMobileMenuOpen(false)}
          >
            {t('landing.browseActivities')}
          </Link>
          <Button
            variant="primary"
            fullWidth
            size="large"
            onClick={() => {
              navigate('/auth')
              setMobileMenuOpen(false)
            }}
          >
            {t('nav.login')}
          </Button>
        </>
      )}
    </div>
  </motion.div>
)}
```

Add AnimatePresence wrapper around the mobile menu for smooth animations.

Import motion and AnimatePresence from framer-motion at top of file.
  </action>
  <verify>Mobile menu has 56px touch targets, smooth animation, warm styling</verify>
  <done>Mobile nav has large touch targets, animated menu, warm cream/white styling</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Redesigned Navbar with warm inclusive design</what-built>
  <how-to-verify>
    1. Run: cd frontend && npm run dev
    2. Visit: http://localhost:5173
    3. Desktop (>768px): Verify:
       - Navbar has warm cream background
       - Logo has coral rounded square with white M
       - Nav links have hover states with coral text
       - CTA buttons are coral
    4. Mobile (<768px): Verify:
       - Hamburger icon is 48px+ touch target
       - Menu opens with smooth animation
       - Links have 56px height touch targets
       - Menu has warm white background
    5. Keyboard: Tab through nav, verify visible focus states
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run build` succeeds
- [ ] Desktop nav has warm cream background with coral accents
- [ ] Mobile hamburger is 48px+ touch target
- [ ] Mobile menu links are 56px height
- [ ] All interactive elements have visible focus states
- [ ] Animations respect reduce motion setting
</verification>

<success_criteria>
- All tasks completed
- Human verification approved
- All verification checks pass
- Navigation fully accessible and warm-styled
</success_criteria>

<output>
After completion, create `.planning/phases/12-navigation-redesign/12-01-SUMMARY.md`
</output>
